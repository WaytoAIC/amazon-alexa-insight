# Alexa 产品洞察 · 使用教程

> WaytoAIC · AI电商之路

## 安装插件

1. 打开 Chrome 浏览器。
2. 进入扩展管理页：`chrome://extensions/`
3. 开启右上角「开发者模式」。
4. 点击「加载已解压的扩展程序」。
5. 选择本项目文件夹。

安装完成后，浏览器扩展栏会出现「Alexa 产品洞察」。

## 开始采集

1. 打开插件面板，停留在「设置」页。
2. 选择目标 Amazon 站点，例如 US、UK、DE、JP。
3. 在「竞品 ASIN」输入框中填入 ASIN 或 Amazon 商品链接。
4. 选择需要采集的问题分类。
5. 如有额外问题，在「自定义问题」中逐行填写。
6. 根据需要调整「采集策略」：
   - 稳定判定次数：回答连续多少次无变化后判定完成。
   - 检测间隔：每隔多久检查一次 Alexa 回答。
   - 超时上限：单个问题最长等待时间。
7. 点击「开始采集」。

插件会自动打开对应 Amazon 商品页，调起 Alexa（原 Rufus），逐个问题采集回答。

## 查看进度

采集开始后会自动切换到「进度」页。

这里可以查看：

- 总体进度
- 当前 ASIN
- 当前问题
- 已采集数量
- 活动日志

如果需要临时停止，可以点击「暂停」或「停止」。

## 导出结果

采集完成后会自动切换到「结果」页。

你可以：

- 按 ASIN 筛选结果
- 按问题分类筛选结果
- 导出 CSV
- 导出 JSON
- 清除结果

CSV 文件可直接用 Excel 或表格工具打开。

## 常见问题

### 没有识别到 ASIN

请确认输入内容包含标准 10 位 ASIN，例如 `B0XXXXXXXX`，也可以直接粘贴 Amazon 商品链接。

### Alexa 没有打开或没有回答

Amazon 页面结构可能变动。可以点击左下角「高级设置」，调整入口、输入框、发送按钮和回答容器选择器。

### 采集速度慢

Alexa 回答需要等待生成完成。可以适当降低「稳定判定次数」或「检测间隔」，但不建议设置过低，避免回答未完成就进入下一题。

### 结果在哪里

结果会保存在浏览器本地存储中，也会在完成后自动导出 CSV。你也可以在「结果」页手动导出。
